generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model cache {
  key        String @id @db.VarChar(255)
  value      String @db.MediumText
  expiration BigInt

  @@index([expiration], map: "cache_expiration_index")
}

model cache_locks {
  key        String @id @db.VarChar(255)
  owner      String @db.VarChar(255)
  expiration BigInt

  @@index([expiration], map: "cache_locks_expiration_index")
}

model events {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  title        String    @db.VarChar(255)
  description  String?   @db.Text
  event_date   DateTime  @db.Date
  location     String?   @db.VarChar(255)
  ticket_link  String?   @db.VarChar(500)
  image_path   String?   @db.VarChar(500)
  sort_order   Int       @default(0) @db.UnsignedInt
  is_published Boolean   @default(true)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
}

model failed_jobs {
  id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String   @unique(map: "failed_jobs_uuid_unique") @db.VarChar(255)
  connection String   @db.VarChar(255)
  queue      String   @db.VarChar(255)
  payload    String   @db.LongText
  exception  String   @db.LongText
  failed_at  DateTime @default(now()) @db.Timestamp(0)

  @@index([connection, queue, failed_at], map: "failed_jobs_connection_queue_failed_at_index")
}

model gallery_categories {
  id             BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  name           String           @db.VarChar(255)
  slug           String           @unique(map: "gallery_categories_slug_unique") @db.VarChar(255)
  sort_order     Int              @default(0) @db.UnsignedInt
  is_published   Boolean          @default(true)
  created_at     DateTime?        @db.Timestamp(0)
  updated_at     DateTime?        @db.Timestamp(0)
  gallery_images gallery_images[]
}

model gallery_images {
  id                  BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  gallery_category_id BigInt?             @db.UnsignedBigInt
  title               String?             @db.VarChar(255)
  image_path          String              @db.VarChar(500)
  alt_text            String?             @db.VarChar(255)
  sort_order          Int                 @default(0) @db.UnsignedInt
  is_published        Boolean             @default(true)
  created_at          DateTime?           @db.Timestamp(0)
  updated_at          DateTime?           @db.Timestamp(0)
  gallery_categories  gallery_categories? @relation(fields: [gallery_category_id], references: [id], onUpdate: NoAction, map: "gallery_images_gallery_category_id_foreign")

  @@index([gallery_category_id], map: "gallery_images_gallery_category_id_foreign")
}

model job_batches {
  id             String  @id @db.VarChar(255)
  name           String  @db.VarChar(255)
  total_jobs     Int
  pending_jobs   Int
  failed_jobs    Int
  failed_job_ids String  @db.LongText
  options        String? @db.MediumText
  cancelled_at   Int?
  created_at     Int
  finished_at    Int?
}

model jobs {
  id           BigInt @id @default(autoincrement()) @db.UnsignedBigInt
  queue        String @db.VarChar(255)
  payload      String @db.LongText
  attempts     Int    @db.UnsignedSmallInt
  reserved_at  Int?   @db.UnsignedInt
  available_at Int    @db.UnsignedInt
  created_at   Int    @db.UnsignedInt

  @@index([queue], map: "jobs_queue_index")
}

model migrations {
  id        Int    @id @default(autoincrement()) @db.UnsignedInt
  migration String @db.VarChar(255)
  batch     Int
}

model password_reset_tokens {
  email      String    @id @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)
}

model sessions {
  id            String  @id @db.VarChar(255)
  user_id       BigInt? @db.UnsignedBigInt
  ip_address    String? @db.VarChar(45)
  user_agent    String? @db.Text
  payload       String  @db.LongText
  last_activity Int

  @@index([last_activity], map: "sessions_last_activity_index")
  @@index([user_id], map: "sessions_user_id_index")
}

model users {
  id                BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name              String    @db.VarChar(255)
  email             String    @unique(map: "users_email_unique") @db.VarChar(255)
  email_verified_at DateTime? @db.Timestamp(0)
  password          String    @db.VarChar(255)
  remember_token    String?   @db.VarChar(100)
  image_path        String?   @db.VarChar(500)
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
}

model videos {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  title        String    @db.VarChar(255)
  youtube_url  String    @db.VarChar(500)
  description  String?   @db.Text
  sort_order   Int       @default(0) @db.UnsignedInt
  is_published Boolean   @default(true)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
}
